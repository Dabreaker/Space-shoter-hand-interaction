# Get this from: Vercel Dashboard → Storage → Blob → your store → ".env.local" tab
# Must start with: vercel_blob_rw_
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxx

# Local port (optional)
PORT=5000

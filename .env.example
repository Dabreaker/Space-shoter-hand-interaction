# Paste your Vercel Blob token here for cloud storage
# Get it from: Vercel Dashboard → Storage → Blob → your store → .env.local
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxx

# Local port (optional, default 5000)
PORT=5000
